info:
  name: Log In
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/business/login
  body:
    type: json
    data: |-
      {
        "contactEmail": "struvesburgs@example.com",
        "password": "P@ssw0rd"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
