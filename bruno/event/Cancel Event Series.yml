info:
  name: Cancel Event Series
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:3000/event?id=1&seriesId=1
  params:
    - name: id
      value: "1"
      type: query
    - name: seriesId
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
