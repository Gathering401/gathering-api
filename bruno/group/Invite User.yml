info:
  name: Invite User
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:3000/group/invite-user?userId=2&id=1
  params:
    - name: userId
      value: "2"
      type: query
    - name: id
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
