info:
  name: Update Role
  type: http
  seq: 11

http:
  method: PUT
  url: http://localhost:3000/group/change-role?id=1&userId=2&role=3
  params:
    - name: id
      value: "1"
      type: query
    - name: userId
      value: "2"
      type: query
    - name: role
      value: "3"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
