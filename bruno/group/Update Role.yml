info:
  name: Update Role
  type: http
  seq: 11

http:
  method: PUT
  url: http://localhost:3000/group/change-role
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
