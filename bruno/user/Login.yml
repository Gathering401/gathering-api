info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/login
  body:
    type: json
    data: |-
      {
        "username": "tstruve",
        "password": "P@ssw0rd"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
